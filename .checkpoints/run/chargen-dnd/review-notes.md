# chargen-dnd — report review notes

Ben's findings while reviewing the delivered app, one at a time. The
accept/reject decision is deferred to the end of the review; nothing here
is acted on until Ben says which items to revise.

1. **Roster note "(by default — created before campaigns named their
   game)"** — Ben asks why it is there. It renders when the campaign view
   says `inferred: true`: an undeclared directory holding characters is
   read as PF2e (spec req 1) and the note makes that inference visible.
   Open: keep, reword, or drop.
