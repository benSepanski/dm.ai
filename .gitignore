/target
node_modules/
ui/test-results/
ui/playwright-report/
.DS_Store

# reference-check ground-truth cache: fetched at tool-time, never committed
.reference-cache/

# live app data in the repo root (scratch campaigns) — never committed
campaign/
campaign.backup-pre-0.2.0/

# per-machine tooling state
.cheese/
.claude/launch.json
