//! The browser copy of the engine: one serde-tagged request enum in, one
//! response enum out, with tsify-generated TypeScript declarations. The UI
//! imports only the thin TS façade over this module.
#![forbid(unsafe_code)]
