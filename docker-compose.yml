version: "3.9"

services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: dmuser
      POSTGRES_PASSWORD: dmpass
      POSTGRES_DB: dmdb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dmuser -d dmdb"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  api:
    build:
      # Root context so the image can install the sibling game-engine package.
      context: .
      dockerfile: dm-api/Dockerfile
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://dmuser:dmpass@postgres:5432/dmdb
      REDIS_URL: redis://redis:6379
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./dm-api/src:/app/src
    # Single worker on purpose: the WebSocket fan-out registry is in-process.
    command: sh -c "alembic upgrade head && uvicorn dm_api.main:app --host 0.0.0.0 --port 8000 --reload"

  ui:
    build:
      context: ./dm-ui
      dockerfile: Dockerfile
    environment:
      # The vite dev server proxies /api (HTTP + WebSocket) to this target.
      VITE_API_URL: http://api:8000
    ports:
      - "5173:5173"
    depends_on:
      - api
    volumes:
      - ./dm-ui/src:/app/src

volumes:
  postgres_data:
